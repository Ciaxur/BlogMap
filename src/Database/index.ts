import { AuthorModel, IAuthor } from './Author.model';
import { IPaper, IPaperType, PaperModel } from './Paper.model';

// Source of Truth, if import needs to be a General Database
export {
  AuthorModel, IAuthor,
  IPaper, IPaperType, PaperModel,
};